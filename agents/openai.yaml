interface:
  display_name: "BERTopic Tuning"
  short_description: "面向短网络文本与长文本的BERTopic调参、同义词停用词管理与持续迭代"
  default_prompt: "使用 $bertopic-tuning 为我的语料执行以主题多样性为核心的 BERTopic 调参与模型迭代，并支持审核同义词、停用词和自定义词表。"
